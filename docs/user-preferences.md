---
name: User preferences and workflow style
description: How this user likes to work, their decisions on features and workflow
type: user
---

- Uses subagent-driven-development for implementation (1 subagent per task, spec review + quality review after each)
- Prefers bilingual docs: every .md doc gets a _cn.md Chinese translation (except SKILL.md files)
- Makes product decisions quickly and clearly when asked; doesn't need options re-explained
- Uses Claude Code superpowers skills (writing-plans, subagent-driven-development, etc.)
- npm is at `/Users/vyodels/.nvm/versions/node/v20.20.2/bin/npm`; load with `export NVM_DIR="$HOME/.nvm" && source "$NVM_DIR/nvm.sh"`
- Primary working language in UI/code is Chinese (Simplified); English for code identifiers and commit messages
